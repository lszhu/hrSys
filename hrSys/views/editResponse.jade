extends layout
block content
  h3 upload info
  ul
    each val, index in userMessage
      - if (val && typeof val == 'object')
        each v, i in val
          li= i + ' = ' + v
      - else
        li= index + ' = ' + val